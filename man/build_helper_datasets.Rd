% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{build_helper_datasets}
\alias{build_helper_datasets}
\title{Build Helper Datasets from Raw RDS Files}
\usage{
build_helper_datasets(years, dir = "./data-raw")
}
\arguments{
\item{years}{A numeric vector of years used to filter the input \code{.rds} files by year in their file paths.}

\item{dir}{A character string specifying the directory containing the raw \code{.rds} files.
Defaults to \code{"./data-raw"}.}
}
\value{
No return value.
}
\description{
Processes raw \code{.rds} files from a specified directory, filters them based on year and size,
combines matching files into unified datasets, saves them as \code{.rda} files, and generates
corresponding documentation entries in \code{./R/helper_data.R}.
}
\details{
This function:
\itemize{
\item Filters \code{.rds} files in the target directory to those matching specified years.
\item Excludes files larger than 1MB and those containing "rate" in the filename.
\item Binds rows of files with matching names across years into a single dataset.
\item Saves each dataset to the \code{./data/} folder as \code{.rda} files.
\item Writes or appends roxygen-style documentation entries to \code{./R/helper_data.R}.
\item Renames the existing \code{helper_data.R} file with the current date before overwriting.
}

Each resulting dataset will be available for use with \code{data()} if the package is rebuilt.
}
\examples{
\dontrun{
build_helper_datasets(years = 2020:2022)
}
}
